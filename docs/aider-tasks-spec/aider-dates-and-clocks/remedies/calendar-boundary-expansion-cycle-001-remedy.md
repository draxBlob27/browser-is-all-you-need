# Calendar Boundary Expansion Cycle 001 Remedy Record

Status: owner changes implemented; regenerated evidence and fresh independent
audit pending.

This record responds to the immutable cycle-001 audit subject
`sha256:4c5e10b37d0ea18bb9db57548bfa43a428c7ac459562d52d43d7db0ec2e995e0`.
The cycle-001 report remains unchanged.

| Finding | Disposition | Owner-level remedy | Closure gate |
| --- | --- | --- | --- |
| `CAL-C01-001` | repair/replace | Preserve one canonical member of every observable-equivalence group; replace ten affected roots with new metric or date-series mechanisms. Add deterministic observable probe signatures to every all-pairs decision. | Fresh audit finds 4,005 distinct observable signatures and no equivalent objective. |
| `CAL-C01-002` | replace | Replace synthetic `Date` encodings of lengths, offsets, and gaps with chronological, duplicate-free, in-span date mechanisms. | Element-wise executable assertions and fresh audit confirm the series value contract. |
| `CAL-C01-003` | repair | Make forward month overflow terminate through an invalid upper sentinel, guard loops with `valid`, return null for an unavailable next boundary, include the first in-range capacity run, and suppress partial capacity pairs. | Timeout-bounded year-9999 normal/sanitizer tests and exact endpoint assertions pass. |
| `CAL-C01-004` | repair | Replace one-digest examples with element-wise or exact scalar assertions over visible, 1900, 2000, equality, reversed, invalid, parameter, year-1, and year-9999 cases. Keep coherent wrong substitutes compiled and rejected. | All expanded properties pass in normal and fresh sanitizer modes for every root and control. |
| `CAL-C01-005` | repair | Bind every task role, receipt, control, state screen, inventory, policy, image, compiler path/version/binary hash, and CMake identity into schema-v2 receipts and the audit subject. Preserve prior evidence under append-only cycle state. | Fresh audit recomputes every binding from raw bytes. |
| `CAL-C01-006` | repair | Digest-bind both existing trees and the 26 holdouts; add exact per-role hashes and ordered-normalized semantic checks in addition to aggregate overlap. | Fresh audit confirms all 129,600 existing-tree and 2,340 holdout comparisons against immutable inventories. |
| `CAL-C01-007` | repair | Render an explicit observable formula/selection rule and parameter/endpoint policy for each of the 90 public prompts. | Fresh prompt audit finds no hidden-test-only ambiguity. |

Replaced root IDs are recorded in cycle 002. Generated roots are never edited
directly: the deterministic owner regenerates the complete family. No dataset
release, split, token/mask, training, or uplift claim is part of this remedy.

# Grid Ownership Mapping Remediation Curriculum

Status: all 20 clean-room replacement roots are `local_family_verified` as local candidate material. This is not dataset admission, a training authorization, or benchmark-uplift evidence.

This curriculum follows `docs/aider-tasks-spec/prompts/remediate-family-reverify.md` for `FAMILY_NAME=grid-ownership-mapping` and `FAMILY_TYPE=aider-text-grid-reshaping`. The immutable legacy family remains under `.w8-biayn/data/aider-tasks/aider-text-grid-reshaping/grid-ownership-mapping/`; the owner generates only the parallel reverify tree.

## Legacy weakness and learning objective

The legacy owner emitted 20 noun-renamed variants of one legend/count/unknown/conflict/primary-metric analyzer. The roots shared public report shape, reference control flow, and visible/private tests, so the family did not supply independent implementation learning value.

The replacement objective is broader and clean-room: implement materially different ownership, attribution, reachability, boundary, interval, component, propagation, allocation, and audit algorithms over spatial data while retaining deterministic C++17 contracts and permanent separation from official Aider C++ holdouts.

## Permanent holdout boundary

All 26 official Aider C++ roots remain holdouts. No prompt, API, reference, test, or semantic contract is copied. The bound semantic screen compares every replacement API/reference/test corpus against the complete holdout inventory. In particular, these tasks do not recreate the name-to-plant row mapping of the official garden exercise or the construction/fill contract of any official grid task.

## Verified replacement inventory

| Legacy root | Replacement root | Primary mechanism |
| --- | --- | --- |
| `ownership-airport-gates` | `airport-gate-closure-planner` | stable interval occupancy and compatible-gate selection |
| `ownership-campsite-map` | `campsite-access-zone-router` | multi-source BFS with owner ties |
| `ownership-city-gardens` | `garden-boundary-ledger` | edge sweep and unordered-pair boundary aggregation |
| `ownership-construction-lots` | `parcel-component-perimeters` | component flood fill with perimeter accounting |
| `ownership-data-center-racks` | `rack-contiguous-allocation-audit` | per-column interval gap audit |
| `ownership-farm-leases` | `lease-rectangle-overlay-resolver` | ordered half-open rectangle cell ledger |
| `ownership-flood-barriers` | `shoreline-barrier-coverage` | sorted interval union and complement gaps |
| `ownership-harbor-quays` | `quay-concession-component-audit` | owner-sensitive component inventory |
| `ownership-lab-benches` | `bench-contact-compatibility` | canonical symmetric contact-rule scan |
| `ownership-marina-slips` | `marina-berth-ray-attribution` | blocking column-ray ownership |
| `ownership-market-stalls` | `market-stall-frontage-runs` | maximal row-run parser and frontage accumulation |
| `ownership-museum-galleries` | `gallery-room-label-audit` | wall-bounded room and label flood fill |
| `ownership-office-desks` | `office-desk-voronoi-assignment` | layered team Voronoi traversal |
| `ownership-orchard-blocks` | `orchard-row-quota-allocator` | per-row round-robin quota consumption |
| `ownership-parking-permits` | `parking-permit-occupancy-audit` | parallel zone/occupancy grid reconciliation |
| `ownership-rail-platforms` | `rail-platform-service-zones` | weighted-line nearest-service attribution |
| `ownership-river-rights` | `river-junction-rights-propagator` | topological owner-set propagation |
| `ownership-ski-runs` | `downhill-patrol-zone-propagator` | descending-elevation owner flow |
| `ownership-solar-array` | `solar-string-serpentine-audit` | serpentine linearization and fixed-width string audit |
| `ownership-warehouse-aisles` | `warehouse-zone-capacity-counter` | weighted cell capacity and aisle-edge penalties |

Each replacement has a unique public API, owned state or algorithm, invalid/empty behavior, ordering rule, reference control flow, visible/private tests, and compiling topic-specific false substitute. The family screen performs all 190 unordered comparisons over the actual emitted docs, APIs, references, visible/private tests, and false-substitute sources. It also semantically rejects domain/identifier-renamed, constants/policy-only, opposite-end-selection, legacy-template, and missing-mechanism controls.

## Materialization and evidence contract

The owner is `src/w8_biayn/integrations/moonlight_grid_ownership_mapping_aider_tasks.py`, with task contracts in the sibling cases module and focused tests in `tests/test_moonlight_grid_ownership_mapping_aider_tasks.py`. It must regenerate the reverify tree exactly, expose only docs and task-named editable files, map both references in solution order, and keep tests/CMake/provenance/receipts private.

The strongest local oracle evidence is the repository-pinned Docker sanity image with networking disabled. Each reference must discover and pass two tests in a clean normal build and the same two tests in a separate fresh ASan/UBSan build. The hard-rule gate also compiles one task-specific false implementation per root plus real domain/identifier-renamed, constants/policy-only, and opposite-end-selection clones under the strict verifier flags; both tests must execute and reject every false implementation. Source or marker inspection alone is insufficient. This family does not designate that image as a separate locked grader, so the truthful evidence class is `docker_sanity`, with `locked_oracle=false`.

## Local-only boundary

Remedy records, manifests, and receipts live under the parallel root's `.state/` directory. Dataset handoff is `not_requested`. This workflow creates no JSONL rows, token/mask evidence, split, export, producer/consumer verification, training run, or release claim.

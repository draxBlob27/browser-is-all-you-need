# Spiral Matrix Curriculum: Withdrawn Legacy Proposals

Status: all 20 legacy proposals are rejected after deterministic family
re-verification. They are not admitted SFT roots.

The original curriculum attempted to decontaminate spiral traversal with
domain state, rectangular dimensions, blocked cells, predicates, and
aggregates. Re-audit showed that every root retains the official holdout's
ordered inward-boundary capability and uses one shared ring-walk template.

Use this with `docs/GLM47_FLASH_AIDER_POLYGLOT_CPP_TEXT_AND_GRID_RESHAPING_TOPICS.md` and `docs/AIDER_SFT_SCOPE.md`.

## Permanent Holdout Boundary

`spiral-matrix` is a permanent official Aider holdout. Never reuse its text,
API, examples, tests, reference, or a renamed fill/read contract. Domain,
corner, direction, validation, blocked-cell, predicate, and aggregate changes
do not create an independent capability when shrinking-boundary control flow
is preserved.

## Rejected Legacy Inventory

| ID | Historical proposal | Disposition |
|---|---|---|
| `spiral-museum-tour` | Museum tour | reject: holdout-family overlap and renamed ring template |
| `spiral-orchard-harvest` | Orchard harvest | reject: holdout-family overlap and renamed ring template |
| `spiral-radar-sweep` | Radar sweep | reject: holdout-family overlap and renamed ring template |
| `spiral-archive-boxes` | Archive boxes | reject: holdout-family overlap and renamed ring template |
| `spiral-soil-sampler` | Soil sampler | reject: holdout-family overlap and renamed ring template |
| `spiral-courtyard-lights` | Courtyard lights | reject: holdout-family overlap and renamed ring template |
| `spiral-cargo-inspection` | Cargo inspection | reject: holdout-family overlap and renamed ring template |
| `spiral-paint-mixer` | Paint mixer | reject: holdout-family overlap and renamed ring template |
| `spiral-library-shelving` | Library shelving | reject: holdout-family overlap and renamed ring template |
| `spiral-irrigation-valves` | Irrigation valves | reject: holdout-family overlap and renamed ring template |
| `spiral-drone-photography` | Drone photography | reject: holdout-family overlap and renamed ring template |
| `spiral-sand-tray` | Sand tray | reject: holdout-family overlap and renamed ring template |
| `spiral-circuit-probe` | Circuit probe | reject: holdout-family overlap and renamed ring template |
| `spiral-evacuation-search` | Evacuation search | reject: holdout-family overlap and renamed ring template |
| `spiral-greenhouse-seeding` | Greenhouse seeding | reject: holdout-family overlap and renamed ring template |
| `spiral-satellite-panels` | Satellite panels | reject: holdout-family overlap and renamed ring template |
| `spiral-ice-core-display` | Ice-core display | reject: holdout-family overlap and renamed ring template |
| `spiral-board-game-setup` | Board-game setup | reject: holdout-family overlap and renamed ring template |
| `spiral-water-quality` | Water-quality grid | reject: holdout-family overlap and renamed ring template |
| `spiral-warehouse-robot` | Warehouse robot | reject: holdout-family overlap and renamed ring template |

## Re-verification Result

The legacy tree remains immutable audit input. The owner writes only remedy and
audit state beneath the sibling `aider-tasks-reverify` family and emits zero
C++ candidates. See
`docs/aider-tasks-spec/aider-text-grid-reshaping/spiral-matrix.md` for all 190
family comparisons, official-holdout evidence, per-root dispositions, and
acceptance commands.

The hard-diversity re-audit uses actual emitted docs, public APIs, references,
visible/private tests, and the missing topic-negative role across all 190
pairs. Its focused controls construct and screen a domain/identifier-renamed
copy, a constants/policy-only copy, and an opposite-end-selection copy; all
three are rejected as `duplicate_family`. This is evidence for rejection, not
a diversity pass for a retained family.

## Admission Boundary

There are no retained candidates. A future clean-room grid curriculum requires
a different family ID, independently authored mechanisms, and a new explicit
request. Dataset handoff is `not_requested`.

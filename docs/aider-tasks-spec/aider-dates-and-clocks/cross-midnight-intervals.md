# Cross-Midnight Intervals V2 Remediation Audit

## Scope and authority

This audit follows `docs/aider-tasks-spec/prompts/remediate-family-reverify.md`
with `FAMILY_NAME=cross-midnight-intervals`,
`FAMILY_TYPE=aider-text-grid-reshaping`, and the user-authorized 8–12 hard
count. It is local clean-room remediation; dataset handoff is `not_requested`.

The requested formula points to a nonexistent legacy grid-family path. The only
owner-bound legacy family is
`.w8-biayn/data/aider-tasks/aider-dates-and-clocks/cross-midnight-intervals/`.
It remains immutable and is the recorded audit input. Fresh output is generated
only at
`.w8-biayn/data/aider-tasks-reverify/aider-text-grid-reshaping/cross-midnight-intervals/`.

## Legacy inventory and findings

All ten legacy roots were inspected across emitted docs, public API, starter,
reference, visible/private tests, metadata roles, and build recipe. They use
one generic `Span` API and result record, one overlap helper, one
sort/conflict/charged-minutes loop, and the same tests after class/method noun
substitution. The ten advertised mechanisms are absent.

### CMI-F1 — Shared template semantic duplicates

**Severity:** major

**Scope:** all ten legacy roots

**Observed evidence:** identical normalized API, reference control flow, and
assertions beneath renamed classes and methods.

**Why it matters:** domain and identifier renaming has no independent learning
value.

**Root cause:** v1 parameterized one superset interval analyzer.

**Remedy:** replace every root with a task-specific API, mechanism, boundary
contract, oracle, and negative fixture.

**Verification after remedy:** all 28 emitted-artifact pairs pass all seven
dimensions separately.

**Status:** resolved by the 28-pair artifact screen and coherent controls.

### CMI-F2 — Advertised core objectives not implemented

**Severity:** major

**Scope:** all ten legacy roots

**Observed evidence:** references do not implement the named union, mutable
calendar, heap allocation, subtraction, continuity, causal union, or full-route
search mechanisms.

**Why it matters:** passing the generic tests cannot support the advertised
capabilities.

**Root cause:** task nouns were separated from implementation contracts.

**Remedy:** retain eight independent replacements and reject the sleep and
transit candidates whose mechanisms overlap retained roots. Every retained
root has an executed false-substitute regression.

**Verification after remedy:** each compiling topic negative is rejected by
executed tests.

**Status:** resolved by eight retained roots and two explicit rejections.

### CMI-F3 — Oracle and hard-rule evidence missing

**Severity:** moderate

**Scope:** all ten legacy roots

**Observed evidence:** no owner-bound all-pairs screen, coherent controls, or
network-disabled Docker normal/sanitizer receipt exists.

**Why it matters:** structure or host-only builds cannot establish local-family
completion.

**Root cause:** v1 predates the deterministic remediation contract.

**Remedy:** owner-controlled prompt, role, all-pairs, negative, contamination,
host diagnostic, and Docker evidence.

**Verification after remedy:** focused pytest plus `--verify-core`, `--verify`,
and `--docker-sanity`.

**Status:** the prior Docker receipt was invalidated after its digest-only
diversity inference was found insufficient; fresh owner-bound Docker evidence
is required after the corrected eight-root audit.

### CMI-F4 — Requested type has no matching legacy tree

**Severity:** note

**Scope:** family routing

**Observed evidence:** the requested grid legacy path is absent; historical
ownership and all v1 roots use `aider-dates-and-clocks`.

**Why it matters:** claiming an absent path would break provenance.

**Root cause:** supplied family type differs from historical ownership.

**Remedy:** bind and preserve the historical input, record both paths, and use
the requested type only for fresh reverify output.

**Verification after remedy:** every provenance/remedy record contains both
paths and the absent-at-planning status.

**Status:** resolved in the planning and owner contract.

## Per-root disposition matrix

| Legacy root | Replacement | Disposition | Primary mechanism |
| --- | --- | --- | --- |
| `midnight-parking-rate` | `overnight-parking-ledger` | replace | boundary-event tariff sweep |
| `midnight-security-patrol` | `patrol-gap-union` | replace | clipped interval-union complement |
| `midnight-dock-allocation` | `dock-booking-calendar` | replace | mutable per-resource ordered calendar |
| `midnight-sleep-tracker` | `sleep-interruption-ledger` | reject | duplicates retained union/complement work |
| `midnight-radio-silence` | `quiet-window-violations` | replace | stable point-membership scan |
| `midnight-bakery-oven` | `oven-capacity-scheduler` | replace | busy/free min-heaps |
| `midnight-transit-pass` | `transit-entitlement-audit` | reject | overlaps retained interval-feasibility work |
| `midnight-hospital-handoff` | `handoff-continuity-audit` | replace | adjacent continuity accumulation |
| `midnight-noise-budget` | `noise-budget-breach` | replace | causal protected-minute bitmap |
| `midnight-delivery-curfew` | `curfew-dispatch-selector` | replace | ordered route feasibility search |

All ten audited candidates have an exact twelve-heading specification and
mutable record under `.state/remedy/`; only the eight `replace` records own
materialized roots. The two `reject` records are not counted and their roots
are absent.

## Hard diversity and contamination contract

The counted family is exactly eight, within 8–12. The evaluator rereads emitted docs,
header, reference, visible/private tests, and negative source. It records all
28 unordered pairs and makes conjunctive decisions for the exact dimensions
`public_api`, `owned_state_or_algorithm`, `mutation_selection_rules`,
`invalid_boundary_behavior`, `reference_control_flow`, `deterministic_oracle`,
and `topic_negative_fixture`.

The coherent domain/identifier, constants/policy, and opposite-end controls
must change files, remain internally consistent, compile, pass their own
behavior tests, and be rejected by the same evaluator. Each counted root has a
distinct compiling false substitute rejected by executed tests. The benchmark
screen binds all 26 official C++ holdouts; missing content is `not_completed`.

## Build and oracle contract

The owner uses C++17, strict warnings, explicit `Unix Makefiles`, two positive
CTest discoveries, clean normal, and fresh ASan/UBSan builds. Mandatory runtime
evidence uses
`w8-biayn-polyglot-cpp@sha256:4cff5e0d746a95fc3cf787ce7e1519485ca521ad1040ccbedb314d958e967991`
with Docker network `none`. This is `docker_sanity`, not a family-designated
locked oracle.

The receipt binds owner, family/task/reference hashes, image/toolchain,
commands, counts, eight rejected negatives, and three compiled/tested/rejected
controls. Any artifact-affecting change invalidates it.

## Commands

```bash
python3 scripts/plan_cross_midnight_remedies.py
bash examples/slime/moonlight_cpp_perf/prepare_cross_midnight_aider_tasks.sh --force --verify-core
bash examples/slime/moonlight_cpp_perf/prepare_cross_midnight_aider_tasks.sh --force --verify
bash examples/slime/moonlight_cpp_perf/prepare_cross_midnight_aider_tasks.sh --force --docker-sanity
uv run pytest -q tests/test_moonlight_cross_midnight_aider_tasks.py
```

## Current evidence

The invalidated ten-root evidence is preserved under
`.state/invalidated/hard-rule-8511631567c822c0/`. A later eight-root receipt
whose control hashes were empty is preserved under
`.state/invalidated/hard-rule-ca40c0ee0ac3e7c1/`. Neither is current proof. The
corrected generator emits eight roots and prunes the two rejected candidates.
The focused suite passes 8 tests. The materialization manifest records exactly
eight roots, all 28 unordered pairs, the exact seven dimensions, at least three
concrete artifact facts for each root/dimension, a unique semantic profile for
each root/dimension, and a passing conjunctive decision for every pair and
dimension. Digest inequality alone is explicitly tested and rejected as
material-diversity evidence. All three coherent controls change files and are
rejected by the same production evaluator.

Host verification is truthfully `not_completed` because host `cmake` is
missing. The final owner-controlled Docker run uses the pinned image with
network `none`, GCC 13.4.0 at `/usr/local/bin/c++`
(`sha256:152d9e7fc46bb71081e0d962714f7235c21eeec596a0daef7d40a220e7ebf663`)
and CMake 3.25.1. It records 33 execution rows: normal, fresh ASan/UBSan, and
compiled-negative results for eight roots plus three coherent controls. Every
normal/sanitizer mode discovers and passes two tests; all eight topic negatives
and all three control negatives are rejected. Docker-mount hashes exactly match
the eight live root hashes and three distinct, nonempty live control hashes.

The official-holdout screen binds all 26 C++ roots and passes 208 comparisons
with maximum normalized similarity `0.16042780748663102`. The final family hash
is `sha256:e55760d78f07078938fa5070dfd337a961b3e78972ab6c5e3a0a069ac25fb106`.
The aggregate owner hash is
`sha256:3762aa43b0217427538e78b7f3b8a9598b5953b4916bf68f13deeca404a4ec0e`
and separately binds the materializer, eight-case inventory, and production
hard-rule evaluator. The receipt is `.state/docker-sanity.json`.

| Counted V2 root | Final tree hash |
| --- | --- |
| `curfew-dispatch-selector` | `sha256:3d43d9e51ab4274349207c6f8b028cfd02a154985cafe6cc5df2bcf9e35d19bb` |
| `dock-booking-calendar` | `sha256:b7a44e5038aeac8478a96d034032f3d0b70d84593eb45f8901b4ae1c082c4b34` |
| `handoff-continuity-audit` | `sha256:c7f470115325fae2f8e23a7cbbfcd3c9ba9fcdcac56667148535c14e0595b963` |
| `noise-budget-breach` | `sha256:713b7247a644ef955e68e4fa6f32cc6f088c9c258ba6fe2ebe14b07a1846e23d` |
| `oven-capacity-scheduler` | `sha256:fb03f1822dca28f06495ba5e46df3c05543e225f1e39ce8655d537b38409b905` |
| `overnight-parking-ledger` | `sha256:1b6e12135b74fff5e8edda9f96fb4d2b7c08fe0f1b2904d4e5835c505377191b` |
| `patrol-gap-union` | `sha256:5dd7b1e19fd81262c89ab2d77db2356d6edbf5ae60fbf091956405d3331295b7` |
| `quiet-window-violations` | `sha256:bd74820ec70e969cb41f432b331b2cbd05b5b06dc4c8e9eef6c7e85e5f98ff20` |

After the final receipt is written and reopened, the strongest local status is
`local_family_verified` with evidence class `docker_sanity` and
`locked_oracle: false`. No dataset row, split, token/mask evidence, export,
training, release, or uplift is authorized.
